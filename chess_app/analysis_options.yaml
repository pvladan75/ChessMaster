include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    unused_field: ignore
    unused_element: ignore
    unused_local_variable: ignore
    avoid_print: ignore

linter:
  rules:
    # avoid_print: false
