# Audit, September 2026 — merged and graded

Four read-only runs on 16.9.2026 against `docs/AUDIT-BRIEF.md`, one per track.
The track files are the evidence: `docs/audit/server.md`, `app.md`,
`contract.md`, `tests.md`. This file is the lead's grading and the proposed order
of work.

**Kept out of git until the findings that read as instructions were fixed**, since
the repository is public; committed on 16.9.2026 after blocks A, B and C and the
owner's four decisions. The one server finding left open by choice is 18, the
RTDN key in a URL (`docs/TODO-objavljivanje.md`, section 6). Scanned for
addresses, hosts and keys before committing.

## Counts

| Track | Critical | High | Medium | Low | Suspicions |
|---|---|---|---|---|---|
| Server: security and data | 2 | 2 | 9 | 6 | 7 |
| App architecture | 0 | 2 | 5 | 5 | 4 |
| Contract app ↔ server | 0 | 5 | 5 | 6 | 3 |
| Test quality | 1 | 4 | 4 | 2 | 6 |

Tracks 1 and 3 found the same root fault in the room's socket layer from two
sides; it is counted once below.

## Graded

**Confirmed** means the lead read both sides of the finding at source.
**Named** means the names or lines were confirmed and the user-visible effect
still needs a run (usually two devices). **Reasoned** means not yet re-derived.

| # | Finding | Source | Status |
|---|---|---|---|
| A1 | Registering an unverified address again keeps the first registrant's password; there is no password reset | server 1 | Confirmed (`routes/auth.js:40-59`; Google path already fixes it at `:316`) |
| A2 | `POST /recordings/save` takes `participants` from the body unchecked | server 2 | Confirmed (`routes/recordings.js:191`) |
| A3 | Socket `move` / `pgn_loaded` never check the sender joined the room | server 3 | Confirmed (`server.js:545`) |
| A4 | `clear_users.js` and `import_new_puzzles.js` `TRUNCATE`/`DROP … CASCADE` whatever `.env` names; `server.js:135` recommends one | server 4 | Confirmed |
| A5 | 100 MB JSON parsed on `/recordings/*` before authentication | server 5 | Confirmed (`server.js:93`) |
| A6 | `POST /rooms/join` answers any guessed code with the whole room row, unlimited | server 6 | Confirmed (`routes/rooms.js:46`) |
| B1 | Room socket names disagree since `6a6b0dd` (10.8.2026): 15 app listeners have no emitter, 5 app emits have no listener; `moveMade` vs `move`, `flip_board_forced` vs `board_flipped`, `lesson_invite` vs `lesson_invite_received` | app 1, contract 1–5, 14 | Named — grep over the whole backend; `pgn_loaded`'s handler never calls `loadFen`, so a peer's board should not follow. Needs a two-device check |
| B2 | Room „Save position": `fen` from the board, `pgn` from the root — the 6.9.2026 fault, still in the room | app 2 | Confirmed (`chess_game_screen.dart:1971`) |
| B3 | „Edit positions" writes `tags: ['lekcija_kurs']` over a tutorial's labels | app 4, 7 | Confirmed (`create_course_dialog.dart:173`) |
| C1 | No test reaches `routes/assignments.js`; its four `trainerOwnsStudent` guards (one in front of the parent report link) can be deleted green | tests 1 | Confirmed (no test file references the route) |
| C2 | The scoped-token refusal in `middleware/auth.js:47` has no test | tests 3 | Confirmed |
| C3 | Ownership predicates tested through fakes that ignore the SQL | tests 2 | Reasoned — prove by mutation on a copy of `chess_backend/` |
| C4 | `POST /login` and the 13+ floor are pinned only as text order or in a service | tests 4, 5 | Reasoned |

Everything else in the four files is **Reasoned** until it is picked up.

## Proposed order

**A. Close the security holes, each with a route test watched failing first.**
✅ Done 16.9.2026, all of A1–A6 and C1, C2, C4 (backend 1289 → 1338); C3 is not
done. See `docs/STANJE-RADA.md`, „Sigurnosni blok iz revizije". Still open from
the server track before these files may be committed (7, `send_lesson_invite`,
was closed by deleting it in block B): 9 and 10 (unmetered Gemini and PDF scans), 11
(emails in logs), 16 (parent-email mail to any address), 17 and 18 (tokens in
URLs), 19 (preview frames outside the queue).
A1, A2, A3 + A6 together (room membership and the code oracle), A4 (a guard that
refuses a non-local database, and delete the `server.js` hint), A5. Then C1, C2
and C4 — the guards that exist but nothing proves. Then commit this file and
`docs/audit/`.

**B. The room.** ✅ Done 16.9.2026 in code (app 2677, backend 1350), including
contract 1–7 and 14 and app 2, 4, 5 and 7; the two-device check is
`TODO-provera.md` item 168. First a two-device session to see what B1 actually costs; then
one pass over the socket contract with a test that reads both ends' event names
(the same shape as the app/server shared fixtures), then B2 and B3.

**C. The rest of the high and medium findings** ✅ Done 16.9.2026 in code (app
2689, backend 1374): server 9–11, 13–17, 19; app 3, 6; contract 9 (lines and
moves only), 10; tests 2, 6–9. Waiting on the owner: server 12 and 18, contract
8, and whether prose over its cap is cut or refused.

**Not planned:** the low findings, until they are next to something being
changed anyway.
